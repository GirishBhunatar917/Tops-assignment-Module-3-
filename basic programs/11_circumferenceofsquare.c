#include<stdio.h>
int main()
{

    int circumference,a,b,c;
    printf("Enter value of a:");
    scanf("%d",&a);

    circumference=4*a;
    printf("circumference of triangle=%d",circumference);
    return 0;
}