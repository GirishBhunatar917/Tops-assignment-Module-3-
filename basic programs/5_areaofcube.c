#include<stdio.h>
int main()
{

    int cube,number,area;
    printf("Enter number:");
    scanf("%d",&number);
    area=6*number*number;
    printf("area of cube is=%d",area);
    return 0;
}